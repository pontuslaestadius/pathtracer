#!/bin/bash

cd $(git rev-parse --show-toplevel)/examples
rm out/*

for file in *.rs
do
    file="${file%.*}"
    cargo run --example $file

    for out in $PWD/out.*
    do
        mv "$out" "out/$file.${out##*.}"
    done
done

